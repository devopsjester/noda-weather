{
  "name": "noda-weather",
  "version": "1.0.0",
  "description": "Gets the local (or specified location's) weather",
  "main": "app.js",
  "scripts": {
    "start": "node app 20850",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Assaf Stone",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.2"
  }
}
