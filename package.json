{
  "name": "noda-weather",
  "version": "1.0.0",
  "description": "Gets the local (or specified location's) weather",
  "main": "app.js",
  "scripts": {
    "run-zip": "node app --zipcode 20850",
    "run-city": "node app --city \"New York City, NY\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Assaf Stone",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.3.1",
    "request": "^2.88.2",
    "yargs": "^17.7.2"
  }
}
